# Azure DevOps (import + optional sync)
# Create a real .env (ignored by git) and set these values.
#
# Org URL example: https://dev.azure.com/***
AZDO_ORG_URL=
AZDO_PROJECT=
# Personal Access Token (PAT)
# - Import requires: Work Items (read)
# - Sync (Kanbanto -> Azure) requires: Work Items (read & write)
# - Comment + attachments requires: Work Items (read & write)
AZDO_PAT=

# Optional: Column -> Azure DevOps System.State mapping
# Use when your Kanbanto column names differ from ADO State names.
# Format: Column=State (comma-separated)
# Example:
# AZDO_COLUMN_TO_STATE=Backlog=New,To Do=To Do,In Progress=Doing,Review=Review,Done=Done
AZDO_COLUMN_TO_STATE=
