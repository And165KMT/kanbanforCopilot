# Example .env (optional)
# This file can be used by the MCP server (mcp/kanbanto-mcp) when it targets this workspace folder.
# Do NOT commit secrets. Use a local `.env` file (this repo ignores it).

# Relative path to the board JSON under the workspace folder
KANBANTO_BOARD_PATH=.kanbanto/tasks.json

# Note: workspace path is set via `--workspace=...` or env `KANBANTO_WORKSPACE` (not via this file).
