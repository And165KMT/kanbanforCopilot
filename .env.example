# Azure DevOps (for MCP import tool)
# Create a real .env (ignored by git) and set these values.
#
# Org URL example: https://dev.azure.com/***
AZDO_ORG_URL=
AZDO_PROJECT=
# Personal Access Token (PAT) with at least Work Items (read)
AZDO_PAT=
